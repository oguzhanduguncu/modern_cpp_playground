to build: cmake -S . -B cmake-build
cmake --build cmake-build

## Yapı

```text
modern-cpp-playground/
├── cpp20/              # C++20 features (ranges, views, modern idioms)
├── performance/        # STL / container performance patterns
├── mini_projects/      # Küçük ama bütünlüklü örnek projeler
├── concurrency/        # (ileride) thread, atomics, cache-level konular
└── CMakeLists.txt
